server:
  port: 8080

spring:
  application:
    name: re-shopping-cart-web-app
  
  # Đã xóa phần profiles active local để chạy được trên server
  
  datasource:
    url: jdbc:mysql://mysql-20a7862e-hieunguyen369248pro-f49e.j.aivencloud.com:14258/defaultdb?useSSL=true&trustServerCertificate=true&serverTimezone=Asia/Ho_Chi_Minh
    username: avnadmin
    password: AVNS_re35sThcXdDXthXgfhZ
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
